# jegorOWL-2
Synth again (but more advanced). This time using Typescript and Svelte for cleaner state management, strict validation of the Web Audio nodes (and to play around with Svelte). Just starting the basic module.

So far we adjust the basic filters, adsr envelope (with a small visualization) and we can play notes. 

next steps: polyphony and delay -> a decent readme -> all the jegorowl magic with owls and minigames.
