# jegorOWL-2
Synth again (but more advanced). This time using Typescript and Svelte for cleaner state management, strict validation of the Web Audio nodes (and to play around with Svelte). Just starting the basic module.

So far we can adjust the basic filters, delay/reverb (newnewnew and shiny),  adsr envelope (with a small visualization) and we can play notes. 

next steps: a decent readme -> all the jegorowl magic with owls and minigames.
